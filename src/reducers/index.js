const initialState = {
    baordWith: 25,
    boardHeight: 25,
  };
  
  export const reducer = (state = initialState, action) => {
    switch(action.type) {
      default:
        return state
    };
  };